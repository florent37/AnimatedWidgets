name: animated_widgets
description: Easily add animations on your screen with AnimatedWidgets. Add always your animated widgets in your component's tree, then activate them when you want to activate the animations.
version: 1.1.1
# authors: [
#   Sayed Ashrf <ssayedaashrf@gmail.com>
# ]
homepage: https://github.com/siko1699/AnimatedWidgets

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: